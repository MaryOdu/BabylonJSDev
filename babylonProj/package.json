{
  "name": "testproj",
  "private": true,
  "version": "0.0.0",
  "type": "module",

  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview --host",

    "lighting01build": "tsc && vite build lighting01",
    "lighting01preview": "vite preview lighting01 --host",

    "lighting02build": "tsc && vite build lighting02",
    "lighting02preview": "vite preview lighting02 --host",

    "rotationbuild": "tsc && vite build rotation",
    "rotationpreview": "vite preview rotation --host",

    "village-Element2build": "tsc && vite build village-Element2",
    "village-Element2preview": "vite preview village-Element2 --host",

    "collision-Element3": "tsc && vite build collision-Element3",
    "collision-Element3preview": "vite preview collision-Element3 --host",

    "gui-Element4": "tsc && vite build gui-Element4",
    "gui-Element4preview": "vite preview gui-Element4 --host"

  },
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^5.4.10"
  }
}
